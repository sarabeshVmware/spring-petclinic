#@ load("_profiles.star", "profiles")

#@ if profiles.is_any_enabled([profiles.full, profiles.workspace, profiles.build, profiles.run, profiles.tbd]):

---
apiVersion: packaging.carvel.dev/v1alpha1
kind: PackageInstall
metadata:
  name: tap-telemetry
  namespace: tap-install
spec:
  serviceAccountName: tap-install-sa
  packageRef:
    refName: tap-telemetry.tanzu.vmware.com
    versionSelection:
      constraints: 0.1.3

#@ end

