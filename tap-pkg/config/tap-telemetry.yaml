#@ load("_profiles.star", "profiles")

#@ if profiles.is_any_enabled([profiles.full, profiles.light, profiles.iterate, profiles.build, profiles.run, profiles.view]):

---
apiVersion: packaging.carvel.dev/v1alpha1
kind: PackageInstall
metadata:
  name: tap-telemetry
  namespace: tap-install
spec:
  serviceAccountName: tap-install-sa
  packageRef:
    refName: tap-telemetry.tanzu.vmware.com
    versionSelection:
      constraints: 0.2.0

#@ end

