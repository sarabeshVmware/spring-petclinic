#@ load("@ytt:data", "data")
#@ load("@ytt:yaml", "yaml")
#@ load("@ytt:struct", "struct")
#@ load("@ytt:overlay", "overlay")
#@ load("_profiles.star", "profiles")

#@ if profiles.is_any_enabled([profiles.full, profiles.light, profiles.view]):
#@ if profiles.is_pkg_enabled("tap-gui.tanzu.vmware.com"):

#@ def collect_values(data_values):
#@  values = {
#@    "ingressDomain": "",
#@  }
#@  if hasattr(data_values, "tap_gui") and data_values.tap_gui:
#@    values.update(struct.decode(data_values.tap_gui))
#@  end
#@  if hasattr(data_values.shared, "ingress_domain") and data_values.shared.ingress_domain:
#@    if not hasattr(data_values, "tap_gui") or not hasattr(data_values.tap_gui, "ingressDomain") or not (data_values.tap_gui.ingressDomain):
#@      values["ingressDomain"] = data_values.shared.ingress_domain
#@      values["ingressEnabled"] = True
#@    end
#@  end
#@  return struct.encode(values)
#@ end
---
apiVersion: packaging.carvel.dev/v1alpha1
kind: PackageInstall
metadata:
  name: tap-gui
  namespace: tap-install
  annotations:
    kapp.k14s.io/change-rule.serviceaccount: "delete before deleting serviceaccount"
    kapp.k14s.io/change-rule.contour: "upsert after upserting contour"
spec:
  serviceAccountName: tap-install-sa
  packageRef:
    refName: tap-gui.tanzu.vmware.com
    versionSelection:
      constraints: 1.2.1
      prereleases: {}
  values:
  - secretRef:
      name: tap-gui-values
---
apiVersion: v1
kind: Secret
metadata:
  name: tap-gui-values
  namespace: tap-install
stringData:
  values.yaml: #@ yaml.encode(collect_values(data.values))

#@ end
#@ end
