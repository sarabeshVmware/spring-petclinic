#@ load("@ytt:data", "data")
#@ load("@ytt:yaml", "yaml")
#@ load("_profiles.star", "profiles")

#@ if profiles.is_any_enabled([profiles.full, profiles.dev, profiles.run]):
#@ if profiles.is_pkg_enabled("appliveview.tanzu.vmware.com"):

---
apiVersion: packaging.carvel.dev/v1alpha1
kind: PackageInstall
metadata:
  name: appliveview
  namespace: tap-install
  annotations:
    kapp.k14s.io/change-rule: "upsert after upserting conventions"
    kapp.k14s.io/change-rule: "delete before deleting serviceaccount"
spec:
  serviceAccountName: tap-install-sa
  packageRef:
    refName: appliveview.tanzu.vmware.com
    versionSelection:
      constraints: 0.4.0-build.1
      prereleases: {}
  values:
  - secretRef:
      name: appliveview-values
---
apiVersion: v1
kind: Secret
metadata:
  name: appliveview-values
  namespace: tap-install
stringData:
  values.yml: #@ yaml.encode(data.values.appliveview)

#@ end
#@ end
