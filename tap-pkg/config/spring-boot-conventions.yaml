#@ load("_profiles.star", "profiles")

#@ if profiles.is_any_enabled([profiles.full, profiles.dev, profiles.build]):
#@ if profiles.is_pkg_enabled("spring-boot-conventions.tanzu.vmware.com"):

---
apiVersion: packaging.carvel.dev/v1alpha1
kind: PackageInstall
metadata:
  name: spring-boot-conventions
  namespace: tap-install
  annotations:
    kapp.k14s.io/change-rule: "upsert after upserting conventions"
spec:
  serviceAccountName: tap-install-sa
  packageRef:
    refName: spring-boot-conventions.tanzu.vmware.com
    versionSelection:
      constraints: 0.2.0

#@ end
#@ end
