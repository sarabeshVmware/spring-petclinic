#@ load("@ytt:data", "data")
#@ load("@ytt:yaml", "yaml")
#@ load("_profiles.star", "profiles")

#@ if profiles.is_any_enabled([profiles.full, profiles.dev_light, profiles.operator_light]):

---
apiVersion: packaging.carvel.dev/v1alpha1
kind: PackageInstall
metadata:
  name: ootb-supply-chain-testing-scanning
  namespace: tap-install
  annotations:
    kapp.k14s.io/change-rule.cartographer: "upsert after upserting cartographer"
    kapp.k14s.io/change-rule.ootb-templates: "upsert after upserting ootb-templates" 
spec:
  serviceAccountName: tap-install-sa
  packageRef:
    refName: ootb-supply-chain-testing-scanning.tanzu.vmware.com
    versionSelection:
      constraints: 0.3.0-build.3
      prereleases: {}
  values:
  - secretRef:
      name: ootb-supply-chain-testing-scanning-values
---
apiVersion: v1
kind: Secret
metadata:
  name: ootb-supply-chain-testing-scanning-values
  namespace: tap-install
type: Opaque
stringData:
  values.yml: #@ yaml.encode(data.values.ootb_supply_chain_testing_scanning)

#@ end
