#@ load("@ytt:data", "data")
#@ load("@ytt:yaml", "yaml")
#@ load("_profiles.star", "profiles")

#@ if profiles.is_any_enabled([profiles.full, profiles.light, profiles.iterate, profiles.build]):
#@ if profiles.is_pkg_enabled("controller.conventions.apps.tanzu.vmware.com"):

#@ def collect_values():
#@  values = {
#@    "ca_cert_data": ""
#@  }
#@  if hasattr(data.values, "convention_controller"):
#@    values.update(data.values.convention_controller)
#@  end
#@  if hasattr(data.values, "shared") and hasattr(data.values.shared, "ca_cert_data") and type(data.values.shared.ca_cert_data) == "string" :
#@    # append shared ca_cert_data to component specific ca_cert_data
#@    values["ca_cert_data"] = values["ca_cert_data"] + data.values.shared.ca_cert_data
#@  end
#@  return values
#@ end

---
apiVersion: packaging.carvel.dev/v1alpha1
kind: PackageInstall
metadata:
  name: conventions-controller
  namespace: tap-install
  annotations:
    kapp.k14s.io/change-group: conventions
    kapp.k14s.io/change-rule.cert-manager: "upsert after upserting cert-manager" 
    kapp.k14s.io/change-rule.serviceaccount: "delete before deleting serviceaccount"
spec:
  serviceAccountName: tap-install-sa
  packageRef:
    refName: controller.conventions.apps.tanzu.vmware.com
    versionSelection:
      constraints: 0.6.1
  values:
  - secretRef:
      name: conventions-controller-values
---
apiVersion: v1
kind: Secret
metadata:
  name: conventions-controller-values
  namespace: tap-install
stringData:
  values.yaml: #@ yaml.encode(collect_values())

#@ end
#@ end
