#@ load("@ytt:data", "data")
#@ load("@ytt:yaml", "yaml")
#@ load("_profiles.star", "profiles")

#@ if profiles.is_any_enabled([profiles.full, profiles.shared_tools]):

---
apiVersion: packaging.carvel.dev/v1alpha1
kind: PackageInstall
metadata:
  name: learning-center-workshops
  namespace: tap-install
  annotations:
    kapp.k14s.io/change-rule: "upsert after upserting learningcenter"
spec:
  serviceAccountName: tap-install-sa
  packageRef:
    refName: workshops.learningcenter.tanzu.vmware.com
    versionSelection:
      constraints: 1.0.8-build.1
      prereleases: {}

#@ end
