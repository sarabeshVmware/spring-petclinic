#@ load("_profiles.star", "profiles")

#@ if profiles.is_any_enabled([profiles.full, profiles.light, profiles.build, profiles.run]):
#@ if profiles.is_pkg_enabled("fluxcd.source.controller.tanzu.vmware.com"):

---
apiVersion: packaging.carvel.dev/v1alpha1
kind: PackageInstall
metadata:
  name: fluxcd-source-controller
  namespace: tap-install
  annotations: 
    kapp.k14s.io/change-rule: "delete before deleting serviceaccount"
spec:
  serviceAccountName: tap-install-sa
  packageRef:
    refName: fluxcd.source.controller.tanzu.vmware.com
    versionSelection:
      constraints: 0.16.0

#@ end
#@ end

