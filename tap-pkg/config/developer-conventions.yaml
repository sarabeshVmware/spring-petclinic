#@ load("_profiles.star", "profiles")

#@ if profiles.is_any_enabled([profiles.full, profiles.iterate]):
#@ if profiles.is_pkg_enabled("developer-conventions.tanzu.vmware.com"):

---
apiVersion: packaging.carvel.dev/v1alpha1
kind: PackageInstall
metadata:
  name: developer-conventions
  namespace: tap-install
  annotations:
    kapp.k14s.io/change-rule.conventions: "upsert after upserting conventions"
    kapp.k14s.io/change-rule.serviceaccount: "delete before deleting serviceaccount"
spec:
  serviceAccountName: tap-install-sa
  packageRef:
    refName: developer-conventions.tanzu.vmware.com
    versionSelection:
      constraints: 0.5.0

#@ end
#@ end
