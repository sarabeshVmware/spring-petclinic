#@ load("@ytt:data", "data")
#@ load("@ytt:yaml", "yaml")
#@ load("@ytt:overlay", "overlay")
#@ load("@ytt:assert", "assert")
#@ load("@ytt:struct", "struct")
#@ load("_profiles.star", "profiles")

#@ if profiles.is_any_enabled([profiles.full, profiles.light, profiles.iterate, profiles.build]):
#@ if profiles.is_pkg_enabled("buildservice.tanzu.vmware.com"):

#@ def values():
#@   validate()
#@   return overlay.apply(default_values(), user_values())
#@ end

#@ def user_values():
#@overlay/match by=overlay.all
#@overlay/match-child-defaults missing_ok=True
--- #@ collect_user_values()
#@ end

#@ def collect_user_values():
#@   values = {
#@     "ca_cert_data": ""
#@   }
#@   values.update(data.values.buildservice)
#@
#@   if data.values.shared.ca_cert_data :
#@     # append shared ca_cert_data to component specific ca_cert_data
#@     values["ca_cert_data"] = values["ca_cert_data"] + data.values.shared.ca_cert_data
#@   end
#@   return values
#@ end

#@ def default_values():
---
#@ if/end profiles.is_any_enabled([profiles.light]):
disable_smart_warmer: true
#@ end

---
apiVersion: packaging.carvel.dev/v1alpha1
kind: PackageInstall
metadata:
  name: buildservice
  namespace: tap-install
  annotations:
    kapp.k14s.io/change-rule: "delete before deleting serviceaccount"
spec:
  serviceAccountName: tap-install-sa
  packageRef:
    refName: buildservice.tanzu.vmware.com
    versionSelection:
      constraints: 1.6.0
  values:
  - secretRef:
      name: buildservice-values
---
apiVersion: v1
kind: Secret
metadata:
  name: buildservice-values
  namespace: tap-install
stringData:
  values.yaml: #@ yaml.encode(values())

#@ end
#@ end
