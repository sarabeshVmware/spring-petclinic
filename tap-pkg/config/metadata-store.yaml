#@ load("_profiles.star", "profiles")
#@ load("@ytt:data", "data")
#@ load("@ytt:yaml", "yaml")
#@ load("@ytt:struct", "struct")

#@ if profiles.is_any_enabled([profiles.full, profiles.view]):
#@ if profiles.is_pkg_enabled("metadata-store.apps.tanzu.vmware.com"):

#@ def collect_values(data_values):
#@  values = {
#@    "ingress_domain": "",
#@  }
#@  if hasattr(data_values, "metadata_store") and data_values.metadata_store:
#@    values.update(struct.decode(data_values.metadata_store))
#@  end
#@  if hasattr(data_values.shared, "ingress_domain") and data_values.shared.ingress_domain:
#@    if not hasattr(data_values, "metadata_store") or not hasattr(data_values.metadata_store, "ingress_domain") or not (data_values.metadata_store.ingress_domain):
#@      values["ingress_domain"] = data_values.shared.ingress_domain
#@      values["ingress_enabled"] = 'true'
#@      values["app_service_type"] = 'ClusterIP'
#@    end
#@  end
#@  return struct.encode(values)
#@ end

apiVersion: packaging.carvel.dev/v1alpha1
kind: PackageInstall
metadata:
  name: metadata-store
  namespace: tap-install
  annotations:
    kapp.k14s.io/change-rule.cert-manager: "upsert after upserting cert-manager"
    kapp.k14s.io/change-rule.serviceaccount: "delete before deleting serviceaccount"
spec:
  serviceAccountName: tap-install-sa
  packageRef:
    refName: metadata-store.apps.tanzu.vmware.com
    versionSelection:
      constraints: 1.2.2
      prereleases: {}
  values:
    - secretRef:
        name: metadata-store-values
---
apiVersion: v1
kind: Secret
metadata:
  name: metadata-store-values
  namespace: tap-install
stringData:
  values.yaml: #@ yaml.encode(collect_values(data.values))

#@ end
#@ end
