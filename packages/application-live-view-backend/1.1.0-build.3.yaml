---
apiVersion: data.packaging.carvel.dev/v1alpha1
kind: Package
metadata:
  name: backend.appliveview.tanzu.vmware.com.1.1.0-build.3
spec:
  refName: backend.appliveview.tanzu.vmware.com
  version: 1.1.0-build.3
  releaseNotes: |
        Release 1.1.0-build.3 of the App Live View package
  releasedAt: 2022-03-17T00:00:10Z
  valuesSchema:
    openAPIv3:
      title: backend.appliveview.tanzu.vmware.com values schema
      properties:
        ingressEnabled:
          type: boolean
          description: |
            Flag for whether or not to create an HTTPProxy for ingress.
          default: false
        ingressDomain:
          type: string
          description: |
            Domain to be used by the HTTPProxy ingress object. The "appliveview"
            subdomain will be prepended to the value provided. For example:
            "example.com" would become "appliveview.example.com".
          default: tap.example.com
        tls:
          type: object
          description: |
            Object for specifying TLS certificate delegation.
          properties:
            namespace:
              type: string
              description: |
                The targeted namespace for secret consumption by the HTTPProxy.
            secretName:
              type: string
              description: |
                The name of secret for consumption by the HTTPProxy.
  template:
    spec:
      fetch:
      - imgpkgBundle:
          image: dev.registry.tanzu.vmware.com/app-live-view/application-live-view-install-bundle@sha256:3f8b933bad3e71754691ab97dd456d9ecafa212e859db914f96341a3d5596b4b

      template:
      - ytt:
          paths:
          - "config"
          - "values.yaml"
      - kbld:
          paths:
          - "-"
          - ".imgpkg/images.yml"
      deploy:
      - kapp: {}
